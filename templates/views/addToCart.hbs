<div class="container">
	<div>
    <h1>Shopping Cart</h1>
    <hr/>
		<h2>{{product.name}}</h2>
		<img src="{{cloudinaryUrl data.product.image}}" alt="Card image cap">
	{{!--	<h2>{{{data.product.description}}}</h2>  --}}
    
		{{!-- <div>{{product.name}}</div> --}}
    
      <label for="quantity">Quantity:{{post}}</label>      
    
	</div>
</div>