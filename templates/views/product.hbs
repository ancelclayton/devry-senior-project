{{!< default}}
<div class="container">
	<div class="jumbotron">
		<h1>{{product.name}}</h1>
		<img src={{product.image.secure_url}} alt={{product.name}} />
		<h2>{{{product.description}}}</h2>
    <a href="/purchase/{{product.slug}}" class="btn btn-primary" >Purchase</a>
	</div>
</div>
